document.addEventListener('DOMContentLoaded', () => {
    const foodList = document.getElementById('food-list');
    const loadFood = document.getElementById('load-food');

    loadFood.addEventListener('click', () => {
        // Code to load more food items goes here
    });

    // Code to generate initial food items goes here
});